apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: aegonish
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: mongo
  clusterIP: None
